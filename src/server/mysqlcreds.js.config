//when asked by phpstorm upon opening this newly created file, set file association for .config to "text"

const creds = {
    host     : 'localhost',
    user     : 'root',
    password : '',
    database : 'c2.19db',
    port: 3306
};

module.exports = creds;